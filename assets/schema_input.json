{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://raw.githubusercontent.com/LaurenHuet/oceanomics-oceangenomes-genomenotes/master/assets/schema_input.json",
    "title": "LaurenHuet/oceanomics-oceangenomes-genomenotes pipeline - params.input schema",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "errorMessage": "Sample name must be provided and cannot contain spaces",
                "meta": ["id"]
            },
            "hifi_reads": {
                "type": "string",
                "format": "directory-path",
                "errorMessage": "HiFi reads directory path must be provided and cannot contain spaces"
            },
            "hic_reads": {
                "type": "string",
                "format": "directory-path",
                "errorMessage": "HiC reads directory path must be provided and cannot contain spaces"
            },
            "assembly": {
                "type": "string",
                "format": "directory-path",
                "errorMessage": "Assembly directory path must be provided and cannot contain spaces"
            },
            "busco_genes": {
                "type": "string",
                "format": "directory-path",
                "errorMessage": "BUSCO genes directory path must be provided and cannot contain spaces"
            }
        },
        "required": ["sample", "hifi_reads", "hic_reads", "assembly", "busco_genes"]
    }
}